\ProvidesPackage{preamble}

    \usepackage[utf8]{inputenc}
    \usepackage[spanish]{babel}
    \usepackage{blindtext}


    \usepackage{fancyhdr}
    \setlength{\headheight}{15.2 pt}
    \pagestyle{fancy}
        \renewcommand{\chaptermark}[1]{\markboth{#1}{}}   %Para que aparezcan sólo los nombres de los capítulos y las secciones
        \renewcommand{\sectionmark}[1]{\markright{#1}{}}
    \fancyhf{}
    \fancyhead[CO]{\nouppercase{\textit{\rightmark}}}
    \fancyhead[CE]{\nouppercase{\textit{\leftmark}}}
    \fancyhead[LE,RO]{\thepage}
    

    \fancypagestyle{plain}{
    \fancyhf{} % clear all header and footer fields
    \fancyfoot[C]{} % except the center PARA QUITAR EL NÚMERO PÁGINA de las primeras páginas de los capítulos
        \renewcommand{\headrulewidth}{3pt}  %Línea horizontal
        
        \renewcommand{\footrulewidth}{0pt}}

        \renewcommand{\headrulewidth}{3pt} %Línea horizontal de las  demás páginas

        \renewcommand{\footrulewidth}{3pt}

    \usepackage{graphicx}
    %\usepackage{mathptmx}

    \usepackage{afterpage}

    \newcommand\blankpage
    {
        \null
        \thispagestyle{empty}
        \addtocounter{page}{-1}
        \newpage
    }
   %Lo anterior para obtener páginas en blanco.

    %Espacio entre párrafos
    \setlength{\parskip}{0.01cm}
    %Espacio entre líneas
    \renewcommand{\baselinestretch}{1}
   \usepackage[Bjornstrup]{fncychap}
        \ChTitleVar{\centering \fontsize{30}{80}\bf \fontfamily{lmss}\selectfont}
        \ChNumVar{\fontsize{150}{80}\usefont{OT1}{pzc}{m}{n}\selectfont}
        





    \usepackage{amsmath}
    \usepackage{amsthm}
    \usepackage{amsfonts}
    \usepackage{amssymb}
    \usepackage{graphicx}
    \usepackage{mathrsfs}
    %\usepackage{geometry}
   
    \usepackage{subcaption}
    \usepackage{float}   %Sirve para poner la H en el ambiente Figure, los flotantes
    \usepackage{wrapfig} %Ambiente wrapfigure
    \usepackage{pstricks} %colores
    \usepackage{enumerate} %listas
    \usepackage{caption} %Fig en vez de Figura
    \usepackage{thmtools}
    
%*******************************************************************************
    %\newtheorem{teo}{Teorema}[chapter]
    %\newtheorem{lema}[teo]{Lema}
    %\newtheorem{cor}[teo]{Corolario}
    %\newtheorem{prop}[teo]{Proposición}
    
    \declaretheorem
    [
        style=plain,
        thmbox={style=L},
        name=Teorema,
        within=chapter,
    ]{teo}
    
    \declaretheorem
    [
        style=plain,
        thmbox={style=L},
        name=Lema,
        within=chapter,
    ]{lema}
   
     \declaretheorem
    [
        style=plain,
        thmbox={style=L},
        name=Corolario,
        within=chapter,
    ]{cor}
    
    \declaretheorem
    [
        style=plain,
        thmbox={style=L},
        name=Proposición,
        within=chapter,
    ]{prop}

    \declaretheorem
    [
        style=definition,
        thmbox={style=S, bodystyle= \normalfont},
        name=Ejemplo,
        within=chapter,
    ]{ejem}
    
    
    
    %\theoremstyle{definition}
        %\newtheorem{defi}{Definición}
        %\newtheorem*{nota}{Notación}
        %\newtheorem*{defin}{Definición}
        %\newtheorem*{obs}{Observación}
        %\newtheorem{ejem}{Ejemplo}[chapter]
        %\newtheorem*{dems}{Demostración}   
    \renewenvironment{proof}{{\bfseries Demostración:}}{\hfill \qedsymbol}
    \renewcommand{\qedsymbol}{\bf{Q.E.D.}}

 %************************************************************************   
    \newcommand{\bond}{$\mathcal{B}(G)$ \hspace{0.1mm}}
    \newcommand{\bondt}{$\mathcal{B}(G)$}%Bond Space
    \newcommand{\cycle}{$\mathcal{C}(G)$ \hspace{0.1mm}}
    \newcommand{\cyclet}{$\mathcal{C}(G)$}%Cycle Space   
    
    \usepackage{imakeidx}
        \makeindex

    \usepackage{scalerel}
        \DeclareMathOperator*{\difsym}{\scalerel*{\triangle}{\textstyle\sum}}
        
    \usepackage{lmodern}   
    \usepackage{lettrine}

    \usepackage[backend=biber, style=numeric ,sorting=nyt]{biblatex}

    %\usepackage{apacite}
    %\usepackage{csquotes}
    
    \nocite{*} %Permite que aparezcan las referencias sin citarlas en el texto
    \addbibresource{ref.bib}